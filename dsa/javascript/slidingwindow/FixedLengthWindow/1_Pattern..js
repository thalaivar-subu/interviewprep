// Build first window

for (...) {
    // add first k elements
}

processWindow();

while (right < n) {
    // Add new element
    add(s[right]);

    // Remove old element
    remove(s[left]);

    left++;
    right++;

    processWindow();
}